import React from 'react';
import { Icon } from 'antd';

const NotFound = () => (
  <h1 style={{ textAlign: 'center' }}>
    <Icon type="exclamation-circle" /> Sorry, that route does not seem to exist any longer
  </h1>
);

export default NotFound;
